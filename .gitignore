# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Local runtime artifacts
outputs/
uploads/
logs/
*.log

# Local env and secrets
.env
.env.*
!.env.example

# Local tooling/system files
.DS_Store
Thumbs.db
.idea/
.vscode/
*.lock

.agent-config/
/.claude/

agent-config.local.yaml

# Personal scratch / paper drafts (kept out of public fork to protect
# double-blind submission anonymity and keep the fork tooling-focused)
# Drop box for files handed to agents; keep the folder and its README
# (must be todo/*, not todo/ -- git cannot re-include a file under an excluded directory)
todo/*
!todo/README.md
